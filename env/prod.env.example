# ================================================================
# 生产环境 — /www/dazistar
# 域名: http://www.dazistar.com  →  PM2 dazistar  →  端口 3000
# 部署: bash scripts/deploy-prod.sh
# ================================================================

PORT=3000
NODE_ENV=production

NEXT_PUBLIC_APP_URL="http://www.dazistar.com"
NEXTAUTH_URL="http://www.dazistar.com"

# DATABASE_URL="postgresql://dazistar:密码@localhost:5432/dazistar"

# 生产环境关闭 MOCK
# ID_VERIFY_MOCK=false
