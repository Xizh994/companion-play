# 搭子星测试环境修复记录 — 2026-05-06

## 问题诊断

测试环境（test.dazistar.com → 3001端口）无法访问。

**根本原因**：PM2 v6.0.14（Go 守护进程）存在架构 bug——Go daemon 抢先占用应用端口，导致 Next.js 进程无法绑定 127.0.0.1:3001。

## 解决步骤

1. **降级 PM2**：v6 → v5.4.3（Node.js daemon），cluster 模式改为 fork 模式
2. **修复 ecosystem.config.js**：移除 `HOSTNAME` 环境变量（不支持），改用 `-H 0.0.0.0` 命令行参数
3. **修改 Nginx 配置**：将 upstream 从 `http://127.0.0.1:3001` 改为 `http://[::1]:3001`（Next.js fork 模式下监听 IPv6）

## 最终配置

**ecosystem.config.js（fork 模式）**：
- `exec_mode: 'fork'`（非 cluster）
- `args: 'start -p 3000'`（3001同理）
- `env.NODE_ENV: 'production'`（无 HOSTNAME）

**Nginx 反向代理**：
```nginx
server {
    listen 80;
    server_name test.dazistar.com;
    location / {
        proxy_pass http://[::1]:3001;
        ...
    }
}
```

## 结果验证

| 环境 | 状态 |
|------|------|
| 生产 (www.dazistar.com → 3000) | HTTP 200 ✅ |
| 测试 (test.dazistar.com → 3001) | HTTP 200 ✅ |
| PM2 进程 | 2个 online，fork 模式，内存正常 |

## 关键经验

- PM2 v6 的 Go daemon 在 cluster 模式下有端口绑定问题，fork 模式正常
- Next.js fork 模式监听 `:::port`（IPv6 only），Nginx 应 proxy_pass 到 `[::1]:port`