---
description: 小改动完成后自动提交并推送，无需再向用户确认
alwaysApply: true
---

# 小改动自动提交推送

完成用户请求的代码修改后，若改动规模**不大**，在通过基本检查（类型检查/相关 lint）后**直接 `git commit` 并 `git push`**，不必再问「要不要提交」。

## 视为「不大」的改动（可直接提交推送）

- 单页 UI/样式调整、文案、小 bug 修复
- 单一 API 或组件的局部逻辑
- 文档、配置示例、Nginx/部署说明的小幅更新
- 涉及文件通常 **≤ 5 个**，且 diff 以局部为主（无全库重构）

## 视为「较大」的改动（先说明或等用户确认再提交）

- 数据库迁移、破坏性 API 变更、大范围重构
- 多模块新功能一次性合并（适合拆 commit 或先汇报）
- 涉及密钥、生产 `.env`、强制部署/回滚操作
- 用户明确说「先别提交」「只改不推」时

## 提交规范

- commit message 使用**中文**（标题一行，空行后可写说明）
- 不要提交 `.env`、密钥、无关的本地草稿
- 推送失败时汇报原因，不要反复 force push

## 汇报方式

提交推送后在回复中简短给出：**提交 hash、一句话摘要**；无需冗长复述 diff。
