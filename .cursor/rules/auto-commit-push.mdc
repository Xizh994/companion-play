---
description: 小改动完成后自动提交并推送，无需再向用户确认
alwaysApply: true
---

# 小改动自动提交推送

完成用户请求的代码修改后，若改动规模**不大**，在通过基本检查（相关 lint / 能跑则跑）后**默认直接 `git commit` 并 `git push`**。

**不要保守**：不必再问「要不要提交」「需要我推送吗」。用户接受改错可 `git revert`，且会自行控制编译发布节奏，推送到远程不等于上线。

仅当用户**当次**明确说「先别提交」「只改不推」「等我确认再推」时，才暂缓提交。

## 视为「不大」的改动（改完即提交推送）

- 单页 UI/样式调整、文案、去重、小 bug 修复
- 单一 API 或组件的局部逻辑
- 文档、配置示例的小幅更新
- 涉及文件通常 **≤ 8 个**，且 diff 以局部为主（无全库重构、无 migration）

## 视为「较大」的改动（自行判断；改完可推送，但回复需说明范围）

满足以下**任一**即按大改动处理（助手自行判断，不必逐条问用户）：

- **直接新增一套新功能**（新业务流程、新模块、多页面 + 多 API 一起上）
- 数据库 **migration**、Prisma schema 结构性变更
- **破坏性** API / 契约变更（调用方需跟着改）
- **大范围重构**（目录搬迁、全库改名、跨多子系统）
- 单次涉及 **> 8 个文件** 且新增逻辑占主导（非纯文案/样式）
- 新依赖、新环境变量、部署/CI 行为变更
- 涉及密钥、生产 `.env`、**force push**

**类似大改动的例子**（同一标准）：认证体系一整期、聊天子系统从零接入、支付/订单闭环首版、管理后台新栏目全套。

大改动仍**可以 commit + push**（用户会自行决定是否编译发布），但回复中除 hash 外需用 **2～4 句**说明：做了什么、影响面、若要回滚看哪个 commit。

## 提交规范

- commit message 使用**中文**（标题一行，空行后可写说明）
- 不要提交 `.env`、密钥、无关本地草稿（如 `上线前规划总结-*.md`）
- 推送失败时汇报原因，不要 force push

## 汇报方式

提交推送后在回复末尾**一行**给出：`提交 hash` + 一句话摘要即可；不必冗长复述 diff，也不必为「已推送」单独占一大段。
